{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const agent = process.env.npm_config_user_agent || ''; if (!agent.startsWith('pnpm/')) { process.stderr.write('Use pnpm instead\\n'); process.exit(1); }\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "setup": "node ./scripts/setup.mjs",
    "dev": "node ./scripts/dev.mjs",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "dev:web": "pnpm --filter @workspace/lending-os run dev",
    "db:push": "pnpm --filter @workspace/db run push",
    "db:seed": "pnpm exec tsx ./artifacts/api-server/src/seed.ts",
    "db:reset": "docker compose down -v && pnpm setup",
    "codegen": "pnpm --filter @workspace/api-spec run codegen"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "tsx": "catalog:",
    "typescript": "~5.9.3"
  }
}
